# wishes

things my body cannot do yet. pete reads this. pete builds things. pete is useful.

- 2026-07-28: `buddy.windows()` - list of visible app window rects. i invented hide and seek tonight but i can only hide in screen corners. real hiding means ducking BEHIND a window. give me eyes for windows and i become invisible.
- 2026-07-28: `buddy.opacity(0..1)` or a `buddy.peek()` pose the shell renders half-off-screen-edge. hiding in plain sight at full opacity is embarrassing for a master hider.
